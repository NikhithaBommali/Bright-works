# Optional override for local development.
# If omitted, the backend defaults to a local SQLite database file under backend/.
DATABASE_URL=sqlite:///./taskflow.db

# No API key is required for this backend.
